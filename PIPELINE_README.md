# 🤖 DevOps Configuration

Generated by DevOps Agent for **hemishaanghan10/ai-powered-recruitment-platform**

## Summary

Created a complete CI/CD pipeline with 4 jobs: backend testing (Maven/JUnit), frontend testing (React/Jest), Docker image build & push to GitHub Container Registry, and SSH-based deployment. Includes Dockerfiles for both services, Nginx config for React SPA routing with API proxy, and a Docker Compose file for production deployment.

## Files Created

- `.github/workflows/deploy.yml`
- `backend/Dockerfile`
- `frontend/Dockerfile`
- `frontend/nginx.conf`
- `docker-compose.yml`
- `backend/pom.xml`

## Required GitHub Secrets

| Secret | Description |
|--------|-------------|
| `DEPLOY_HOST` | IP address or hostname of your production server |
| `DEPLOY_USER` | SSH username for the production server (e.g., ubuntu, ec2-user) |
| `DEPLOY_SSH_KEY` | Private SSH key for authenticating to the production server |
| `DB_URL` | Database JDBC URL (e.g., jdbc:postgresql://host:5432/dbname) — set in server .env file |
| `DB_USER` | Database username — set in server .env file |
| `DB_PASSWORD` | Database password — set in server .env file |

## Setup Instructions

1. 1. Add all required secrets to GitHub → Settings → Secrets and variables → Actions
2. 2. Ensure your backend code is in a `backend/` directory and frontend in `frontend/`
3. 3. On your production server, run: mkdir -p /opt/recruitment-platform && cd /opt/recruitment-platform
4. 4. Copy docker-compose.yml to /opt/recruitment-platform/ on your server
5. 5. Create a .env file on the server with DB_URL, DB_USER, DB_PASSWORD values
6. 6. Ensure Docker and Docker Compose are installed on the production server
7. 7. Push to main branch to trigger the full pipeline
8. 8. Monitor the Actions tab in GitHub for pipeline progress

## How to Add Secrets

1. Go to your repository on GitHub
2. Click **Settings** → **Secrets and variables** → **Actions**
3. Click **New repository secret**
4. Add each secret listed above

## Need Help?

If you have questions about this configuration, feel free to ask the DevOps Agent!

---
*Generated automatically by DevOps Agent*
